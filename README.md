# Pandoc Converter

Инструмент для пакетной конвертации документов с использованием [Pandoc](https://pandoc.org/).

## 📋 Описание проекта

**Pandoc Converter** — это Python-утилита для автоматизированной конвертации больших объёмов документов между различными форматами:

- **DOCX → Markdown** — извлечение текста из Word-документов в чистый Markdown
- **Markdown → DOCX** — создание Word-документов из файлов Markdown
- **PDF → DOCX** — конвертация текстовых PDF в редактируемые Word-файлы

Утилита работает в пакетном режиме: загружаете папку с десятками файлов — получаете результат в несколько секунд.

### Особенности

- ✅ Пакетная обработка десятков файлов за раз
- ✅ Автоматическая установка Pandoc
- ✅ Простая структура папок (input → output)
- ✅ Сохранение структуры документов (заголовки, списки, таблицы)
- ✅ Кроссплатформенность (Windows, Linux, macOS)

---

## 🚀 Быстрый старт

### Системные требования

- **Python 3.8+** ([скачать](https://www.python.org/downloads/))
- Подключение к интернету (для первой установки)

---

## 📥 Шаг 1. Загрузка проекта

### Клонирование репозитория

Откройте терминал (Windows: PowerShell, Linux/macOS: Terminal) и выполните:

```bash
git clone https://github.com/mischembelov/pandoc-converter.git
cd pandoc-converter
```

### Проверка содержимого

После клонирования в папке должны быть файлы:

```
pandoc-converter/
├── convert.py           # Основной скрипт конвертации
├── setup.py             # Скрипт автоматической настройки
├── requirements.txt     # Список зависимостей
├── .gitignore
└── README.md
```

---

## ⚙️ Шаг 2. Настройка локального окружения

### 2.1. Создание виртуального окружения

Выполните команды **внутри папки проекта**:

**Windows (PowerShell):**
```powershell
python -m venv .venv
.venv\Scripts\Activate.ps1
```

**Linux / macOS:**
```bash
python3 -m venv .venv
source .venv/bin/activate
```

После активации в начале строки терминала появится `(.venv)` — это означает, что виртуальное окружение активно.

### 2.2. Установка зависимостей

```bash
pip install -r requirements.txt
```

Эта команда установит необходимые Python-библиотеки:
- `pdf2docx` — для конвертации PDF → DOCX
- `pypandoc` — обёртка для работы с Pandoc из Python

### 2.3. Автоматическая настройка проекта

```bash
python setup.py
```

**Что делает эта команда:**

1. 📁 **Создаёт рабочие папки:**
   - `input/` — для исходных DOCX-файлов
   - `input_md/` — для исходных Markdown-файлов
   - `input_pdf/` — для исходных PDF-файлов
   - `output/` — для результата MD-файлов
   - `output_docx/` — для результата DOCX-файлов

2. 📦 **Проверяет и устанавливает зависимости**

3. ⚙️ **Скачивает и устанавливает Pandoc** (если не установлен)

**Ожидаемый вывод:**

```
📁 Создаём рабочие папки...
   ✅ input/
   ✅ input_md/
   ✅ input_pdf/
   ✅ output/
   ✅ output_docx/

📦 Устанавливаем Python-зависимости...
✅ Зависимости установлены

⚙️  Проверяем и устанавливаем Pandoc...
✅ Pandoc уже установлен: v3.1.2

🚀 Готово! Проект настроен.
```

> ⚠️ **Важно:** Если Pandoc не установился автоматически, скачайте его вручную:
> - Windows: https://github.com/jgm/pandoc/releases/latest
> - Linux: `sudo apt install pandoc` (Ubuntu/Debian) или `sudo yum install pandoc` (Fedora/RHEL)
> - macOS: `brew install pandoc`

---

## 🔄 Шаг 3. Использование конвертера

Проект поддерживает три режима работы. Все команды выполняются **из корня проекта** при **активном виртуальном окружении** `(.venv)`.

### Режим 1: DOCX → Markdown

**Назначение:** Извлечение текста из Word-документов в формат Markdown (без изображений).

**Шаги:**

1. Поместите `.docx` файлы в папку `input/`
2. Выполните команду:
   ```bash
   python convert.py to_md
   ```
3. Заберите результат из папки `output/`

**Пример вывода:**

```
📄 Найдено файлов: 15

  ✅ Руководство.docx
  ✅ Руководство 2.docx
  ✅ T3.docx
  ...

✅ Успешно: 15  |  ❌ Ошибок: 0
```

**Особенности:**
- Сохраняются заголовки, списки, таблицы, форматирование
- Изображения **не извлекаются** (только текстовое содержимое)
- Имена файлов сохраняются (например, `file.docx` → `file.md`)

---

### Режим 2: Markdown → DOCX

**Назначение:** Создание Word-документов из Markdown-файлов.

**Шаги:**

1. Поместите `.md` файлы в папку `input_md/`
2. Выполните команду:
   ```bash
   python convert.py to_docx
   ```
3. Заберите результат из папки `output_docx/`

**Пример вывода:**

```
📄 Найдено файлов: 8

  ✅ T3 1.md
  ✅ T3 2.md
  ...

✅ Успешно: 8  |  ❌ Ошибок: 0
```

**Особенности:**
- Markdown-синтаксис преобразуется в форматирование Word
- Поддерживаются таблицы (pipe-tables), блоки кода, списки
- Ссылки и изображения (если указаны в MD) обрабатываются корректно

---

### Режим 3: PDF → DOCX

**Назначение:** Конвертация текстовых PDF в редактируемые Word-документы.

**Шаги:**

1. Поместите `.pdf` файлы в папку `input_pdf/`
2. Выполните команду:
   ```bash
   python convert.py pdf_to_docx
   ```
3. Заберите результат из папки `output_docx/`

**Пример вывода:**

```
📄 Найдено файлов: 5

  ✅ annual_report_2025.pdf
  ✅ technical_specification.pdf
  ...

✅ Успешно: 5  |  ❌ Ошибок: 0
```

**⚠️ Важное ограничение:**

Конвертация работает **только с текстовыми PDF** (где текст можно выделить мышкой).

Для сканированных документов (изображений страниц) требуется **OCR** (оптическое распознавание символов) — это отдельная задача, не входящая в возможности данного инструмента.

---

## 📂 Структура проекта после настройки

```
pandoc-converter/
├── .venv/                  # Виртуальное окружение (не коммитится в Git)
├── input/                  # ← Сюда кладёте .docx для to_md
├── input_md/               # ← Сюда кладёте .md для to_docx
├── input_pdf/              # ← Сюда кладёте .pdf для pdf_to_docx
├── output/                 # ← Отсюда забираете результат .md
├── output_docx/            # ← Отсюда забираете результат .docx
├── convert.py              # Основной скрипт конвертации
├── setup.py                # Скрипт первичной настройки
├── requirements.txt        # Python-зависимости
├── .gitignore              # Исключения для Git
└── README.md               # Документация (этот файл)
```

> 💡 Папки `input/`, `output/` и их содержимое **не загружаются в Git** — они для локальной работы.

---

## 🛠️ Решение проблем

### Ошибка: `pandoc: command not found`

**Причина:** Pandoc не установлен или не добавлен в PATH.

**Решение:**

1. Проверьте установку:
   ```bash
   pandoc --version
   ```

2. Если команда не найдена:
   - **Windows:** Скачайте `.msi` установщик с [pandoc.org](https://pandoc.org/installing.html) и установите
   - **Linux:** `sudo apt install pandoc` (Ubuntu/Debian)
   - **macOS:** `brew install pandoc`

3. После установки перезапустите терминал

---

### Ошибка: `ModuleNotFoundError: No module named 'pdf2docx'`

**Причина:** Не установлены Python-зависимости или виртуальное окружение не активировано.

**Решение:**

1. Убедитесь, что виртуальное окружение активно (в начале строки должно быть `(.venv)`)
2. Запустите:
   ```bash
   pip install -r requirements.txt
   ```

---

### PDF конвертируется некорректно (пустой DOCX или искажённый текст)

**Причина:** PDF является сканом (изображением), а не текстовым документом.

**Проверка:** Откройте PDF и попробуйте выделить текст мышкой:
- ✅ Текст выделяется — конвертация возможна
- ❌ Текст не выделяется — требуется OCR (оптическое распознавание)

---

### Ошибка: `❌ Нет файлов .docx в папке input/`

**Причина:** Файлы лежат не в той папке или имеют неправильное расширение.

**Решение:**

1. Убедитесь, что файлы находятся **непосредственно** в нужной папке:
   - `input/file.docx` ✅
   - `input/subfolder/file.docx` ❌ (подпапки не обрабатываются)

2. Проверьте расширение файлов (должно быть `.docx`, а не `.doc`)

3. Для `.doc` (старый формат) сначала пересохраните в `.docx` через Microsoft Word

---

## 📄 Лицензия

MIT License — свободное использование и модификация.



